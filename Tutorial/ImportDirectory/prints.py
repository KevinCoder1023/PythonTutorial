# As we need to use the functions that belongs to another directory,
# we have to import the directory and declare the functions that we need/want to use
import functions
from functions import printOutText, genRandomNum
# First: Directory, Functions that we need
printOutText()
genRandomNum()